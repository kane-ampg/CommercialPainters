node_modules
.next
out
build
.DS_Store
*.pem
.env
.env*.local
.vercel
next-env.d.ts
/playwright-report
/test-results
/blob-report
/playwright/.cache
tsconfig.tsbuildinfo

# Local tool cache — machine-specific absolute paths and session ids.
.impeccable/

# Master video source. 342MB, 4K, 50Mbps — never deployed and never committed.
# The web encodes in public/video/ are generated from it by
# scripts/encode-hero-video.mjs and are what ship.
/media/

# Third-party locality dataset. The generated output in content/ is committed
# instead, so builds are deterministic and offline.
.cache/
